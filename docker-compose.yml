version: "3.8"

services:
  flask:
    build:
      context: .
    ports:
      - "5000:5000"
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5000 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
